#include "shuai.rpc"
namespace shuai{
service opService
{
	int8 login(string  openid);
	user xixi(user u);
	array<role> lala(map<int32,role> m,array<int32> ai,array<role> ar);
	int8 ping(int8 i8);
};
}